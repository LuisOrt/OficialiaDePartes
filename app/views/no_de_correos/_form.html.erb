<%= form_for(no_de_correo) do |f| %>
  <% if no_de_correo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(no_de_correo.errors.count, "error") %> prohibited this no_de_correo from being saved:</h2>

      <ul>
      <% no_de_correo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 <% if current_user.admin? && NoDeCorreo.last.eql?(nil) %>
  <div class="field">
    <%= f.label :No_de_Correo %>
    <%= f.number_field :No_de_Correo %>
  </div>
  <% else%>
  <div class="field">
    <%= f.hidden_field :No_de_Correo,  :readonly => true %>
  </div>
  <% end%>
  <div class="field">
    <%= f.label :Departamento %>
    <%= f.text_field :Departamento, :readonly => true, :value => current_user.departamento.nombre %>
  </div>
  <div class="field">
    <%= f.label :Proposito %>
    <%= f.text_area :Proposito %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
 <div>
    <%= link_to "Numeros de correo solicitados", no_de_correos_path %>
  </div>
<% end %>
